NOTE:	You may need to change the interfaces' names and 
	the path to CPU temp. Also change "time athens" 
	to your local timezone.

cp config ~/.i3
cp i3status.conf /etc
cp i3-sensible-terminal /usr/bin/
